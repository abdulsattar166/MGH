# Server
PORT=4000

# MySQL (Aiven)
DB_HOST=mysql-1452e18b-practicewithcharlie-df1b.k.aivencloud.com
DB_PORT=22161
DB_USER=avnadmin
DB_PASSWORD=<New-Password>
DB_NAME=defaultdb
DB_SSL=true
# DB_SSL_CA=   (optional, see below)

# Auth: generate a NEW secret (command below), don't reuse the old one
JWT_SECRET=<JWT-Secret>

# Set to "true" to skip the login requirement (everyone gets in as Super Admin).
AUTH_DISABLED=false

# Frontend origin
CORS_ORIGIN=http://localhost:3000